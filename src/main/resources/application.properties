# Server
server.port=${PORT:8080}

# App name
spring.application.name=Alert Service

# Datasource (RDS Postgres) - force SSL
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://alert-db.c7s6aeqiigt5.us-east-2.rds.amazonaws.com:5432/alertdb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:alertadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Dulcolax5hs*}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Mail
spring.mail.host=smtp.example.com
spring.mail.port=587
spring.mail.username=youruser
spring.mail.password=yourpass
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# App flags
app.simulate.slack=true
